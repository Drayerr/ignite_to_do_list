import styled from 'styled-components'

export const Container = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;

  margin-left: 8px;
  gap: 8px;

  font-weight: 700;

  background: var(--blue);

  height: 54px;
  width: 90px;

  border-radius: 8px;
`
